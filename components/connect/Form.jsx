import PropTypes from 'prop-types';

const Form = ( handleCloseEmail ) => {
    return (
        <div>
            form
        </div>
    )
}

Form.propTypes = {
    handleCloseEmail: PropTypes.func.isRequired
}

export default Form;
